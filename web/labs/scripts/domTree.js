function dfs() {
  var frontier = [];
  //frontier.push(start_node);
  while (true) {
    if(frontier.length == 0)
      return null;
    current = frontier.pop();
    //goal_test()
    //frontier.push(get_children);
  }
}
